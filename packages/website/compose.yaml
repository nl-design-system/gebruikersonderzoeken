---
version: "3"
services:
  www:
    volumes:
      - ./dist:/usr/local/apache2/htdocs/
      - ./dist/.htaccess:/usr/local/apache2/htdocs/.htaccess
      - ./httpd.conf:/usr/local/apache2/conf/httpd.conf
    build:
      dockerfile: Dockerfile
    ports:
      - "8080:80"
